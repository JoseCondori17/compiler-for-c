#include <stdio.h>

int main() {
    int x = 20; 
    int y = 10;
    int z = 30;
    if(x > y){
        printf(x);
        return 0;
    }
    if(x < y){
        printf(y)
        return 0;
    }
    printf(z);
    return 0;
}